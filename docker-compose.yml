version: '3'

services:
  nginx:
    image: nginx:alpine
    container_name: v2ray-sub-nginx
    ports:
      - "8080:80"
    volumes:
      - ./output:/usr/share/nginx/html:ro
      - ./nginx-docker.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
